package Qustions.Array;

// public class mcq1 {
//     double getAverage(int arr[], int size);

// int main () {

// int balance[5] = {1000, 2, 3, 17, 50};

// double avg;

// avg = getAverage( balance[0], 5 ) ;  //

// cout << "Average value is: " << avg << endl;

// return 0;

// }
// }
//avg = getAverage(balance[0], 5); is incorrect. balance[0] refers to the first element of the array (which is 1000), but the function getAverage expects the entire array as its first argument, not just a single element.
// You need to pass the entire array (balance), not just balance[0].

